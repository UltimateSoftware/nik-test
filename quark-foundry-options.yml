template: app-gh
branches:
  - name: main
    options:
      - package
      - publish-docker-image
      - upload-artifacts-dev
      - quark-phoenix-docker-image-deploy
name: tms-test-repo
parameters:
  project-dir: ''
type: github-actions
settings-profile: default
